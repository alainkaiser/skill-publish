# skill-publish

Skills live under `skills/<skill-name>/SKILL.md` (YAML frontmatter + Markdown body).

After this repo is pushed to GitHub, install the **no-blocking-async** skill into any project with:

```bash
npx skills add alainkaiser/skill-publish --skill no-blocking-async
```

Browse public skills at [skills.sh](https://skills.sh).
